
/**
 * # DEV ENTRY
 * - Entry file for the local component docs page at `/dev`.
 * - Loads the stylesheet bundle and wires interactive docs examples.
 */

import { initializeDropdownComponents } from '../component/dropdown/dropdown.component';
import './dev.scss';


/**
 * Wires interactive components shown in the static dev docs page.
 */
initializeDropdownComponents();
