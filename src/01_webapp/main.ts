
import { initializeHome } from './home/home.component';
import './main.scss';


/**
 * # MAIN ENTRY
 * - Starts the browser app from the Vite entry file.
 * - Loads the home page controller, which mounts the translator UI.
 */
initializeHome();
