
# PROMPT FOR SANDBOX ARCHITECTURE FOR ELECTRON (v0.4.0)
- Didn't apply the changes, but let's have the prompts for the future reference.





---





Fren, you are an Electron master. I need your help to implement Electron to the existing web app.


## 1. Goal
Move the based-translator source files from `src/01_webapp` to `src/00_shared` so that `src/01_webapp` can be used to build a webapp, and `src/02_electron` can be used to build a native app.


## 2. Context
01. In `src/01_webapp`, we have the existing webapp, based-translator.
	01-01. The webapp uses Vite + TypeScript.
02. In `src/02_electron`, we have a simple example of Electron app where a label follows the mouse cursor.
	02-01. The Electron app uses Vite + TypeScript + Electron Forge.
03. Both are sandboxed completely.
04. Now we have a new folder `src/00_shared` for the based-translator app.
05. The draft file `src/00_shared/package.json` has been added.
	05-01. `openai` dependency has been moved from `src/01_webapp/package.json` to `src/00_shared/package.json`.
06. `src/00_shared` will contain the based-translator app.
	06-01. Including its design, components, etc.
07. `src/01_webapp` will be used to build a web app from `src/00_shared`.
	07-01. It will contain the only the necessary logic to build the app with Vite.
08. `src/02_electron` will be used to build a native app from `src/00_shared`.
	08-01. Electron will be used to add a feature of a real desktop app, such as; showing a label following the mouse cursor.


## 3. Step-By-Step Tasks
01. Analyze and understand the webapp configs.
02. Analyze and understand the Electron app configs, focusing on Electron Forge.
03. Understand the based-translator app.
04. Move the based-translator source files from `src/01_webapp` to `src/00_shared`.
	04-01. Keep the current folder structure of the webapp.
	04-02. `src/01_webapp` will only contain the logic to build the app with Vite.
	04-03. All the files related to the based-translator app will be in `src/00_shared`.
05. Update the configs for the webapp to work like as-is.
	05-01. Update `src/00_shared/package.json` if needed.
	05-02. Update `src/01_webapp/package.json` if needed.
06. Update `package.json` in the root.


## 4. Rules
01. The current webapp has no bugs, so be careful when you change the codes.
	01-01. Minimal changes preferred.
	01-02. If necessary, we can split the current app logic into multiple files.
02. Do not change the source files in `src/02_electron`.
	02-01. Focus on the web app for now.
03. If you must change the webapp source codes, respect the current coding style.
04. `src/02_electron` will be used to wrap the based-translator webapp.
	04-01. We want to add a native app feature like; a label following mouse cursor globally.
05. Since this is Linux environment (no GUI), do not build the app. I will do it by myself.





---





Fren, you are a architecture master. I need your help to find the elegant structure to wrap the existing webapp with Electron to add an OS native feature.


## 1. Goal
Refactor the current architecture of the based-translator app to add an OS native feature where the translated text follows the mouse cursor globally using Electron (`src/02_electron`), maintaining the current web app logic (`src/01_webapp`) as much as possible.


## 2. Context
01. In `src/01_webapp`, we have the existing webapp, based-translator.
	01-01. The webapp uses Vite + TypeScript.
02. In `src/02_electron`, we have a simple example of Electron app where a label follows the mouse cursor.
	02-01. The Electron app uses Vite + TypeScript + Electron Forge.
	02-02. Use the existing code as a reference.
03. Currently, both are sandboxed completely.
04. `src/02_electron` will be used to build a native app from `src/01_webapp`.
	04-01. Electron will be used to add a feature of a real desktop app, such as; showing a translated text following the mouse cursor.
05. In the future, we might want to add another wrapper like `src/03_tauri`.


## 3. Step-By-Step Tasks
01. Analyze and understand the webapp configs.
02. Analyze and understand the Electron app configs, focusing on Electron Forge.
03. Understand the based-translator app.
04. Update the source codes in `src/01_webapp`.
	04-01. Update `src/01_webapp/package.json` if needed.
	04-02. It's okay to split `src/01_webapp/main.ts` into multiple files if needed.
	04-03. Every logic related to the based-translator app stays in this folder.
	04-04. The files in this folder are only for the web app.
	04-05. The logic must be flexible enough to be used by Electron as well as other framework like Tauri.
05. Update the source codes in `src/02_electron` to show the based-translator using Electron.
	05-01. Electron is used to wrap the based-translator webapp.
	05-02. The existing logic is an example code for the mouse cursor following feature, so use it as reference.
	05-03. Electron only cares about OS native features.
	05-04. In the future, we might want to add more OS native features.
06. Update `package.json` in the root.


## 4. Rules
01. The current webapp has no bugs, so be careful when you change the codes.
	01-01. Minimal changes preferred.
02. Respect the current coding style.
03. Sandboxing the webapp is important; if `src/02_electron` is removed, the webapp must be still working 100% fine.
04. Since this is Linux environment (no GUI), do not build the app. I will do it by myself.
05. The best structure is easy-to-delete.





