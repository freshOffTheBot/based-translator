
# PROMPT FOR ADDING ELECTRON (v0.4.0)
- Implement Electron.





---





Fren, you are an Electron master. I need your help to implement Electron's OS-native features from the existing web app.


## 1. Goal
- Update Electron (`src/02_electron`) to add an OS-native feature where the translated text follows the mouse cursor globally by communicating the existing web app (`src/01_webapp`).


## 2. Context
01. In `src/01_webapp`, we have the existing webapp, based-translator.
	01-01. The webapp uses Vite + TypeScript.
02. In `src/02_electron`, we have a simple example of Electron app where a label follows the mouse cursor.
	02-01. The Electron app uses Vite + TypeScript + Electron Forge.
	02-02. Use the existing code as a reference.
03. Currently, both are sandboxed completely.
04. Communicate through `CustomEvent` between the webapp and the Electron app.
	04-01. The `CustomEvent` logic is in the folder `src/01_webapp/common/native-event/`.


## 3. Rules
01. The current webapp has no bugs, so be careful when you change the codes.
02. Minimal changes preferred.
03. Respect the current coding style.
04. Sandboxing the webapp is important; if `src/02_electron` is removed, the webapp must be still working 100% fine.
05. The Electron app (`src/02_electron`) must show the same app as the existing web app.
	05-01. Use the same source code from the web app (`src/01_webapp`).
06. Since this is Linux environment (no GUI), do not build the app. I will do it by myself.
