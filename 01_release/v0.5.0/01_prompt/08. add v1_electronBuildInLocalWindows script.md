
# ADD `v1_electronBuildInLocalWindows` SCRIPT (v0.5.0)
- Add `v1_electronBuildInLocalWindows` script file.





---





Fren, you are a bash script master. I need your help to write `.sh` file.


## 1. Goal
01. Write a bash script file to build an Electron app in a remote Windows environment.



## 2. Context
01. The script file is: `02_script/01_electronBuildInLocalWindows/v1_electronBuildInLocalWindows.sh`.
02. Use the variables like:
	02-01. `$LINUX_TEMP_ZIP_FILENAME=basedTranslator-temp.zip`
	02-02. `$LINUX_FINAL_ZIP_FOLDER_PATH`
	02-03. `$WINDOWS_SSH_ALIAS`
	02-04. `$WINDOWS_PROJ_FOLDER_PATH`
	02-05. `$WINDOWS_NODE_FOLDER_PATH`
	02-06. `$WINDOWS_BUILD_OUTPUT_PATH`
03. The remote Windows machine runs OpenSSH server.
04. The remote Windows commands should run through explicit PowerShell over SSH.
05. On the remote Windows machine, use portable Node.js.
	05-01. `$WINDOWS_NODE_FOLDER_PATH` points to the folder that contains `node.exe` and `npm.cmd`.
06. `$WINDOWS_BUILD_OUTPUT_PATH` starts from `$WINDOWS_PROJ_FOLDER_PATH`.
	06-01. Example: `src/02_electron/out`.



## 3. Workflow
01. Before starting:
	01-01. Validate required variables and local commands: `zip`, `scp`, `ssh`.
	01-02. Show a colored ASCII summary panel.
	01-03. Highlight `yes` in the confirmation text.
	01-04. Start only when the user enters exactly `yes`.
2. Check the Windows project folder.
	02-01. If the folder does not exist, create it.
	02-02. If the folder is empty, proceed.
	02-03. If the folder is not empty, confirm to delete the contents entirely.
	02-04. If the user enters `yes`, delete the contents and proceed.
	02-05. If the user enters anything else, exit the script.
03. Zip the project source files:
	03-01. If the temp zip file already exists, delete the temp zip file.
	03-02. In the project root, run: `zip -r "$LINUX_TEMP_ZIP_FILENAME" . -x "$LINUX_TEMP_ZIP_FILENAME" -x "src/01_webapp/node_modules/*" -x "src/02_electron/node_modules/*" -x ".state/*" -x ".codex/*" -x ".git/*" -x ".worktree/*"`.
04. Copy the temp zip file to the Windows project folder.
	04-01. Use `scp`: `scp "$LINUX_TEMP_ZIP_FILENAME" "$WINDOWS_SSH_ALIAS:$WINDOWS_PROJ_FOLDER_PATH"`.
	04-02. After the temp zip file is copied, delete the Linux temp zip file.
05. Unzip the temp zip file on Windows.
	05-01. Use PowerShell `Expand-Archive`.
	05-02. Unzip into `$WINDOWS_PROJ_FOLDER_PATH`.
	05-03. After unzip, delete the Windows temp zip file.
06. Install webapp dependencies.
	06-01. Prepend `$WINDOWS_NODE_FOLDER_PATH` to the Windows `PATH`.
	06-02. Verify `node.exe` and `npm.cmd` are available.
	06-03. In the Windows project folder, run: `npm run webapp:install`.
07. Install Electron dependencies.
	07-01. In the Windows project folder, run: `npm run electron:install`.
08. Build the Electron app.
	08-01. In the Windows project folder, run: `npm run electron:build`.
	08-02. Verify the folder at `$WINDOWS_PROJ_FOLDER_PATH/$WINDOWS_BUILD_OUTPUT_PATH` exists.
09. Zip the built files.
	09-01. Zip filename: `basedTranslator-portable-v0.0.0.zip`.
	09-02. The version number comes from `"version"` in the root `package.json`.
	09-03. Zip the folder at `$WINDOWS_PROJ_FOLDER_PATH/$WINDOWS_BUILD_OUTPUT_PATH`.
10. Copy the zip file to Linux.
	10-01. If the file already exists in `$LINUX_FINAL_ZIP_FOLDER_PATH`, delete it.
	10-02. Copy the zip file from Windows to `$LINUX_FINAL_ZIP_FOLDER_PATH`.
	10-03. After copy, delete the zip file in Windows.



## 3. Rules
01. Use bash.
02. Locate the variables at the top of the script so that they can be easily changed.
	02-01. If needed, use more variables to make the code quality better.
	02-02. For Linux related variables, use the prefix: `$LINUX_`.
	02-03. For Windows related variables, use the prefix: `$WINDOWS_`.
	02-04. Provide example values and inline comments where helpful.
03. Do not have default value for the variables.
	03-01. If required variables are not set, show an error message and exit the script.
04. At the top of the script, add script-file comment like the below:

```sh
#
#	# ELECTRON BUILD IN LOCAL WINDOWS v1.0
#	- <description here..>
#
#	## Prerequisite
# 	- <prerequisite here..>
#
#	## How To Use
#	01. <add step-by-step here..>
#

<logic goes here..>
```

05. Add inline comments for other dev frens.
06. Print logs for each step.
	06-01. Use colors for beautiful output.
	06-02. Use numbered step logs like `[1/9] <description here..>`.
	06-03. After each successful step, print an [info] log explaining what happened.
	06-04. Cleanup actions can be [info] logs and do not need to increase the step count.
	06-05. No need to have log files, just print.
07. Before starting the process, show a colored ASCII summary panel and confirm from the user.
	07-01. Make sure the ending | characters are aligned even when paths are long.
	07-02. Highlight yes with color in the confirmation text.
08. Ask questions when you find edge cases.

