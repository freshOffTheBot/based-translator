
# ADD COLLAPSE COMPONENT (v0.5.0)
- Add a collapse component to dev.





---





Fren, you are a web app master. I need your help to add a collapse component.


## 1. Goal
01. Add a collapse component to `src/01_webapp/component/collapse/collapse.component.scss`, using SCSS only.
02. Show the component in `src/01_webapp/dev` as `Components > Collapse`.



## 2. Context
01. Understand the file that contains global variables: `src/common/scss/app.variable.scss`.
02. Add a collapse component to the blank file: `src/01_webapp/component/collapse/collapse.component.scss`.
	02-01. Use SCSS only.
	02-02. The main CSS class name is: `c-collapse` (`c-` as a common component).
	02-03. Additional CSS class name is: `c-collapse-*` (`c-collapse-` as prefix).
	02-04. Use semantic HTML contract based on `details` and `summary`.
	02-05. The component should support native open / closed state with no JavaScript.
	02-06. Keep the overall collapse container and trigger area visually minimal.
	02-07. Make only the collapse marker / indicator feel more obviously clickable in the collapsed state.
	02-08. Keep the marker animation when toggling open / closed.
03. Based on the new collapse component, show the component in `src/01_webapp/dev` as `Components > Collapse`.
	03-01. Understand the design reference files: `src/01_webapp/dev/dev.html` and `src/01_webapp/dev/dev.scss`.
	03-02. Add the component to the end of the dev page.
	03-03. Show the exact reusable HTML snippet in the dev docs.
04. If necessary, take a look into other components (`src/01_webapp/component/**/*.component.scss`).



## 3. Rules
01. Do not add additional files.
02. Use dash (`-`) only for CSS classes (no underscore).
03. Respect the existing HTML/SCSS coding styles.
04. Do not make the whole collapsed card stronger or louder; emphasize only the marker button.


