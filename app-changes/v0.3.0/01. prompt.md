
# PROMPT FOR v0.3.0
- For the components in `dev.html`.



## AGENTS.md

```md
# WEB APP
01. This web app uses TypeScript, SCSS, and Vite.



## 1. Project Structure
01. `package.json`: for pnpm.
02. `tsconfig.json`: for TypeScript.
03. `vite.config.ts`: for Vite.
04. `.editorconfig`: project coding style.
05. `src/`: the folder contains all project source files.
06. `src/common`: the folder contains globally-used files.
	06-01. `src/common/scss/app.variable.scss`: global SCSS variables.
07. `src/components`: the folder contains component files.
	07-01. `src/components/card/card.component.scss`: card component SCSS file.
	07-02. `src/components/form/form.component.scss`: form component SCSS file.
08. `src/dev/dev.html`: dev docs for all components.
	08-01. This page shows atomic component and its HTML code.
09. `src/main.scss`: the main SCSS entry file.
10. `src/main.ts`: the main TypeScript entry file.



## 2. Rules
01. Use simple and easy-to-understand codes.
02. Add useful comments so that other dev frens understand the codes super easy.
03. Use single-source-of-truth approach.
04. Great code structure is easy-to-delete.
05. Add semicolons in the source codes.
```



---



## Initial Prompts

```md
Fren, you are a SCSS master. I need your help to create minimal-design components with SCSS.


## 1. Step-By-Step
01. Understand the file: `src/common/scss/app.variable.scss` 100%.
  01-01. Understand the variables 100%.
  01-02. Keep the file as-is (read-only).
02. Create atomic components in these files:
  02-01. `src/components/card/card.component.scss`
  02-02. `src/components/form/form.component.scss`
03. Update the file: `src/dev/dev.html` to show the above components as dev docs.
  03-01. This page is for other dev frens.
  03-02. Show atomic component and HTML code.
  03-03. Benchmark Bootstrap's dev docs page.



## 2. Requirements for `dev.html`
01. Include the section: Colors.
  01-01. Pick the beautiful and elegant theme colors for dark theme.
  01-02. Colors: Foreground, background, border, primary, success, danger, warning, info, muted, ...
02. Include the section: Typography.
03. Include the section: Components > Card.
04. Include the section: Form > Overview.
  04-01. Focus on text controls.




## 2. Rules
01. Do not add additional files.
02. Use the existing variables.
03. Do not update the config files.
04. Use dash (`-`) only for CSS classes (no underscore).
05. Focus only on the dev page and the components. Forget about `main.css` and `main.ts` because they are not part of this task.
```



---



## Additional Prompts

```md
Fren, let's update a few more things:
1. Use green color as primary (i.e., button).
2. For other colors, pick different color (rather than `-500`).
3. (Optional) if necessary, you can create new colors to make things more beautiful and more elegant.
4. There are a few layout issues on dev.html -> fix them.
```



```md
Fren, you are a SCSS master. I need your help to clean up the existing SCSS files.

## 1. Step-By-Step
01. Understand the below files:
  01-01. `src/common/scss/app.variable.scss`
  01-02. `src/components/card/card.component.scss`
  01-03. `src/components/form/form.component.scss`
  01-04. `src/dev/dev.html`
  01-05. `src/dev/dev.scss`
02. Move the button logic from `dev.html`/`dev.scss` to `src/components/button/button.component.scss`
03. Move the typography logic from `dev.html`/`dev.scss` to `src/components/typography/typography.component.scss`


## 2. Rules
01. Respect the current SCSS coding styles.
02. If we need to create more folders, ask me.
  02-01. i.e., `src/components/foo/foo.component.scss`
```




