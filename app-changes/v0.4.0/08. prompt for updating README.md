
# PROMPT FOR UPDATING README.md
- Update `README.md` for v0.4.0.





---





Fren, you are a Github master. I need your help to update the existing `README.md`.



## 1. Goal
01. Update the existing `README.md` to explain the project much better.





## 2. Context To Add
01. Add explanation for each folder:
	01-01. `app-changes/`
	01-02. `docs/`
	01-03. `src/01_webapp/`
	01-04. `src/02_electron/`
02. Add explanation for each file:
	02-01. `.editorconfig`
	02-02. `AGENTS.md`
	02-03. `package.json`
03. Mention that this project is built by Codex, Codex only.
04. Explain the web app and the Electron app.
	04-01. The web app is sandboxed.
	04-02. The web app works without the Electron wrapper.
	04-03. The Electron app is for building `.exe` for Windows environment.
	04-04. The Electron app has an OS-native feature where the translation output follows the mouse cursor globally.
05. Mention that during the development process of the app, the Electron-built `.exe` file will be manually built in a Windows environment.
	05-01. Once the app becomes more stable and useful, we plan to add an automated build process through a GitHub task.
06. Explain our coding principles and philosophy.
	06-01. We stick to the vanilla version because we want to use tools as the creators intended.
	06-02. We pursue minimal dependencies because we want full control over the project.
	06-03. We use a strcuture that's easy for humans to understand, because we're not bots - we're human.
07. Mention that we use the default setup to use tools as the creators intended.
	07-01. By using the default setup, it will be easier to handle migrations if there are major changes in the framework.
08. For the web app config, we use the default setup of vite CLI (v8.3.0)'s vanilla-ts template.
	08-01. `$ npm create vite@8.3.0 myapp -- --template vanilla-ts`
09. For the Electron app config, we use the default setup of Electron Forge (v7.11.1)'s vite-typescript template.
	09-01. `$ npx create-electron-app@7.11.1 myapp --template=vite-typescript`
10. Add a TODO section at the end of the document.





## 3. Rules
01. Respect the existing style in `README.md`.
	01-01. Respect the existing wordings.
02. Use easy and simple English.
03. It must be instantly understandable at a glance for dev frens.


